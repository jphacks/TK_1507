machine:
  node:
    version: 5.0.0

dependencies:
  override:
    - cd client && npm install
    - cd client && npm install -g gulp

    - cd server && bundle install

test:
  override:
    - cd client && npm run build

    - cd server && rake test
